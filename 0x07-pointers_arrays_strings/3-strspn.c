#include "main.h"
#include <stdio.h>

/**
 *  * main - check the code
 *   *
 *    * Return: Always 0.
 */
unsigned int _strspn(char *s, char *accept)
{
	int x;
	int j;

	for( x = 0; accept[x] == s[j]; x++)
		for( j = 0; 

		return (x);
}
