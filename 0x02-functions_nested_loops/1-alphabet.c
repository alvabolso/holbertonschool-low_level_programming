#include "main.h"
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char letras = 'a';

	while (letras <= 'z')
	{
			_putchar(letras);
			letras++;
		}
		_putchar('\n');
		return (0);
}
